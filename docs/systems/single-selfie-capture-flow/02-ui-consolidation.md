# Task 2: UI Consolidation

Use the new capture component in `/quiz` and remove duplicate upload surfaces.

Acceptance:

- `/quiz` offers both choose photo and take photo.
- `/dashboard` redirects to `/quiz`.
- `components/quiz/quiz-selfie-step.tsx` and `components/dashboard/color-analyzer.tsx` are removed.
- Visible links no longer point users to `/dashboard`.

